#!/bin/bash
#SBATCH -N $SLURM_N
#SBATCH -C $SLURM_C
#SBATCH -p $SLURM_p
#SBATCH -J $SLURM_J
#SBATCH -L $SLURM_L
#SBATCH -t $SLURM_t
$SLURM_ADDITIONAL

#OpenMP settings:
export OMP_NUM_THREADS=$OMP_NUM_THREADS
export OMP_PLACES=threads
export OMP_PROC_BIND=spread

#run the application:
$COMMANDS

