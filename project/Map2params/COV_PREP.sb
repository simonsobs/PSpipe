#!/bin/bash
#SBATCH -N 1
#SBATCH -C haswell
#SBATCH -p regular
#SBATCH -J prepare_mcm
#SBATCH -L SCRATCH
#SBATCH -t 12:00:00


#OpenMP settings:
export OMP_NUM_THREADS=64
#export OMP_PLACES=threads
#export OMP_PROC_BIND=spread

#run the application:
srun -n 1 -c 64 --cpu_bind=cores python get_maps_to_params_covariance.py  global_CAR_spin02.dict


